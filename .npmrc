# manim-ts is a local file: dependency. npm symlinks those by default, and
# Turbopack will not follow a symlink out of the project root — so install it
# as a real copy instead. Without this, any npm install breaks the manim engine.
install-links=true
